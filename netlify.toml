# Netlify auto-detects Next.js and applies its runtime, so this file only pins
# the parts worth being explicit about. Runtime league data lives in Netlify
# Blobs and is refreshed by netlify/functions/refresh-espn.ts.
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
